/**
 * @file favoriteCategoryHelpers.ts
 * @description Helper utilities for Favorite Category values.
 */

export const formatFavoriteCategoryName = (name: string): string => {
  return name.trim();
};
